apiVersion: v1
kind: Service
metadata:
  name: fastmenu
  namespace: fastmenu
  labels:
    app: fastmenu
spec:
  type: ClusterIP
  selector:
    app: fastmenu
  ports:
    - name: http
      port: 80
      targetPort: http
